
import React from 'react';
import { useToast } from '@/components/ui/use-toast';

export const NotificationToasts = () => {
  // Este componente é apenas um placeholder para o sistema de notificações
  // As notificações são gerenciadas pelo hook useToast e o componente Toaster no App.tsx
  return null;
};
